targets:
  $default:
    builders:
      drift_dev:
        options:
          databases:
            app: lib/data/db/app_database.dart
          schema_dir: drift_schemas/
          test_dir: test/drift/
          store_date_time_values_as_text: false
